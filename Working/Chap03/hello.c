// hello.c by Bill Weinman <http://bw.org/>

// preprocessor directive
// standard IO
#include <stdio.h>

// main function declaration
// main function is reserved in C
// it is required and where execution begins
int main( int argc, char ** argv ) {
	printf("Hello, World!\n");
	return 0;
}

// in C the ; is a terminator, not a separater so it is required
